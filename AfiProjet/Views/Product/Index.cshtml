@model IEnumerable<AfiProjet.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h2>Index!!!!</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>

<partial name="_dropdown" />



<partial name="_ProductGrid" />



<div class="text-center">
    @if (ViewBag.noPage > 0)
    {
        <a asp-action="index" asp-route-id="@(ViewBag.noPage-1)"><i class="fas fa-arrow-alt-circle-left fa-lg"></i></a>
    }
    @(ViewBag.noPage + 1)
    @if (ViewBag.noPage < ViewBag.nbPages-1)
    {
        <a asp-action="index" asp-route-id="@(ViewBag.noPage+1)"><i class="fas fa-arrow-alt-circle-right fa-lg"></i></a>
    }


</div>

